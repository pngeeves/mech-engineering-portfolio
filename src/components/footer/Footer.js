import "./footer.css";

//login to change to admin so they can change it themselves 
//build out first then change so we can get it live

export default function Footer(){

    return(
        <div className="footer">            
                <h4>@Paula Geeves-Booth</h4>
            
        </div>
    )
}